services:
  midpoint:
    build: .
    container_name: midpoint
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - N8N_ENQUIRY_WEBHOOK=${N8N_ENQUIRY_WEBHOOK}
